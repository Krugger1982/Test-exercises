# Задание 3

import random
def quicksort(nums):
   if len(nums) <= 1:
       return nums
   else:
       q = random.choice(nums)
       s_nums = []
       m_nums = []
       e_nums = []
       for n in nums:
           if n < q:
               s_nums.append(n)
           elif n > q:
               m_nums.append(n)
           else:
               e_nums.append(n)
       return quicksort(s_nums) + e_nums + quicksort(m_nums)
       
# Применен метод быстрой сортировки Хоара (также еще известный как "сортировка слиянием")
# Он предполагает деление массива на две части, в одной из которых находятся элементы меньше определённого значения, в другой – больше или равные
# Ассимптотика по времени - О(NlogN) (если повезет и первый выбранный q является медианным значением для этого массива)
# В худшем случае, когда массив на входе уже отсортированный и первое q попадает в начало или конец массива, тогда скорость алгоритма сравнивается с сортировкой пузырьком
