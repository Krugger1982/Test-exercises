# Задание № 1
# Вариант 1 (текстовое представление чисел - позволяет работать с "Длинными целыми" числами
#            и экономит память)

def  isEven(value):
    value = str(value)
    evens = ['0', '2', '4', '6', '8']
    return value.isdigit() and value[-1] in evens
	#ответом является выполнение условия, что в числе value нет плавающей точки и последняя цифра - четная
  
 # Вариант 2 - побитовое представление чисел
 def  isEven_1(value):
    return bin(value)[-1] == 0
    # в битовом представлении четного числа младший разряд всегда равен 0, 
    # а нечетного  всегда равен 1
